let names = ["Berni", "Emil", "Andi", "Bertold"];

names.forEach((names) =>
{
    $("ul").append(`<li> ${names} </li>`);
});



  $('body').append(
    '<h1>Added with javascript</h1>'
    
  );

  $('body').append(
    `<p>This block was added using JavaScript's jQuery library. How awesome!</p>`
  );
